**boolean**{%if 'title' in schema%} - *{{schema.title}}*{%endif%}
{%-if 'default' in schema%}
 * Default value: `{{schema.default | lower }}`
{%-endif-%}
{%-if 'const' in schema%}
 * Value must be: `{{schema.const | lower }}`
{%-endif-%}